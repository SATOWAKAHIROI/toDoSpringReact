FROM node:22-alpine

WORKDIR /app

# 依存関係をインストール
COPY package*.json ./
RUN npm install

# ソースコード
COPY . .

# ポート (開発サーバ)
EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]